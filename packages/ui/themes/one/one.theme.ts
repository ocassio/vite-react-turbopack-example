import { createTheme } from "@mui/material";
import { orange, purple } from "@mui/material/colors";

export const ONE_THEME = createTheme({
    palette: {
        primary: orange,
        secondary: purple
    }
});
import { createTheme } from "@mui/material";
import { blue, green } from "@mui/material/colors";

export const TWO_THEME = createTheme({
    palette: {
        primary: blue,
        secondary: green
    }
});